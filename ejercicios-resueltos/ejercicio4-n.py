'''Crear una función que reciba los grados
centígrados y regrese los Farenheit'''


def convertir_temperatura(cent):
    #r = 9 / 5
    #c = cent * r
    #res = c + 32
    return (cent * (9/5))+ 32

print(convertir_temperatura(27))
